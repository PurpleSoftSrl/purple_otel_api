name: purple_otel_api
description: OpenTelemetry API for Dart — interfaces, no-op implementations, and semantic conventions. Zero external dependencies.
version: 0.1.0
repository: https://xamapps.visualstudio.com/AI/_git/purple_otel_api
homepage: https://pub.dev/packages/purple_otel_api
topics: [opentelemetry, observability, tracing, logging, metrics]
license: AGPL-3.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
